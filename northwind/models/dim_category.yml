version: 2

models:
  - name: dim_category
    description: Category Dimension. One row per category
    columns:
      - name: categorykey
        description: The surrogate key of the category
        tests:
          - not_null
          - unique
      - name: categoryid
        description: The business / source key of the category
        tests:
          - not_null
          - unique